# Handoff — mm_git

## Current State
- `git` role established in `mm`.
- Repository is clean after the last commit (`c210c1e`).
- Project structure refactored: core in `src/`, scripts in `scripts/`.

## Tasks
- [x] Establish `git` role and scripts.
- [ ] Implement `commit-scope.sh`, `commit-sweep.sh`, `preflight.sh` scripts in `agent/roles/git/`.

## Blockers
- None.
